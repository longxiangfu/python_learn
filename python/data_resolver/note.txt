总结
1、图线的分类（seaborn）：
    普通数量图(柱状图)（x轴为指标，y轴为对应的数据量）countplot;
    散点图scatterplot;
    折线图(线形图)lineplot;

    箱形图boxplot;
    点状图pointPlot

2、对DataFrame数据进行过滤
    直接:  goldMedals = merged[merged.Medal == 'Gold']
    loc[]:  goldMedals = merged.loc[merged['Medal']=='Gold']
